name: schedule event

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  schedules-example:
    runs-on: ubuntu-latest
    steps:
      - name: Echo current time
        run: echo "The current time is $(date)"
